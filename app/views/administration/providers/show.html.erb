<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @provider.name %>
</p>

<p>
  <strong>Vat:</strong>
  <%= @provider.vat %>
</p>

<p>
  <strong>Address:</strong>
  <%= @provider.address.street %>, 
  <%= @provider.address.zipcode %>,
  <%= @provider.address.city %>,
  <%= @provider.address.state %>,
  <%= @provider.address.nation %>
</p>

<%= link_to 'Edit', edit_administration_provider_path(@provider) %> |
<%= link_to 'Back', administration_providers_path %>
